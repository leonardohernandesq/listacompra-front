/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // Isso permitirá que o projeto seja construído mesmo que existam erros de ESLint
      ignoreDuringBuilds: true,
    },
  };
  
  export default nextConfig;
  